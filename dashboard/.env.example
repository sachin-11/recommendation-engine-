# Copy to .env.local. Both are exposed to the browser.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=RecoEngine
# Optional: where the sidebar's "API docs" link points (defaults to <API>/docs).
# NEXT_PUBLIC_DOCS_URL=https://docs.recoengine.io
